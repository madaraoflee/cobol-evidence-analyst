# COBOL Evidence Analyst — 领导演示包

## 新入口：COBOL Lens 本機工作台

公司電腦使用 GitHub 上的項目源碼部署，需要 Python 3.10+。首次下載後，將項目根目錄的 `.env.example` 複製為 `.env`，填入 `COMPANY_API_BASE_URL`、`COMPANY_API_KEY` 和 `COMPANY_CHAT_MODEL`。之後雙擊 `poc\run_web.bat` 即可，無需每次在終端輸入接口設定。`.env` 已被 Git 忽略，更新程式碼時保留本機檔案；若下載到新的目錄，將自己的 `.env` 複製到新項目根目錄。

新工作台以「源碼接入 → 業務問題 → 證據回答 → 匯出結果」為主線，採紅、白、深灰的企業介面，右上角可切換簡體中文、繁體中文或英文，並記住選擇。從項目根目錄啟動：

    python poc\web_app.py --port 8765

Windows 也可執行：

    poc\run_web.bat

服務預設開啟瀏覽器；若沒有自動開啟，可在啟動後進入 [COBOL Lens 本機工作台](http://127.0.0.1:8765)。需要手動開啟時使用 `--no-browser`。不要直接雙擊 `poc/web/index.html` 代替啟動服務。

未接入任何源碼時，畫面提供明確標記的**示例預覽**。切換到本機源碼模式，輸入源碼與輸出目錄，才能建立當次真實索引；程式與數量會依服務回傳更新。建立索引始終離線；按「開始業務分析」才會使用已設定的公司模型接口，傳送問題及有限源碼證據，按鈕旁會明示這個用途。尚未配置時先顯示連線設定說明。公司 API 設定自動讀取項目根目錄的 `.env`，已有進程環境變數可覆蓋同名項目；修改後重啟服務。服務不回傳 API Key，瀏覽器亦不儲存它；接口已設定也不等於能力已驗證。

2026-09-14 更新：接入改為輕量目錄及增量快取，新增三語進度、耗時、完成／剩餘、可估算時的 ETA 和停止；大目錄分頁搜尋，選入口後才詳細解析。缺 COPY／閉源物件時保留局部答案及邊界。操作與实測口徑見[最新手冊](../15-system-user-manual.md)及[性能與局部分析報告](../reports/2026-09-14-large-source-intake.md)。

先看[新版工作台流程、三分鐘演示腳本與驗收邊界](./workbench-ui.md)。公司接口設定及真實源碼讀取參數見[辦公室使用手冊](../11-office-usage-guide.md)。本地畫面和合成資料測試不代替公司真實源碼、實際 API 或 Windows 實機驗收。

本輪已在本機瀏覽器驗證：接入包含兩個程式的測試源碼、查看實際調用關係，以及打開第 8 行的源碼證據。實際公司 API 本次未調用；這個結果證明本機介面已接通相應服務，尚不代表完整業務問答驗收通過。

另以模擬模型回應和真實索引／證據驗證了回答呈現、工具記錄、換題後清空舊結果與證據不足狀態，並核對瀏覽器下載的 Markdown 摘要。當時全套 685 項測試通過；後續增量接入與局部分析的驗證範圍以[2026-09-14 報告](../reports/2026-09-14-large-source-intake.md)為準。

## 舊版演示歸檔

以下 `prototype.html`、截圖及五分鐘講稿保留作歷史記錄。它們使用預設合成案例呈現，沒有連接本機分析服務；輸入另一個問題不代表會重新調查源碼。下文的數量與階段說明屬當時記錄，新工作台的狀態以實際服務回傳為準。

當時的 P1-B 可執行記錄驗證了特定合成案例的本地證據調查；它不代表完整業務問答已交付。相關來源事實仍有缺口，不能把固定頁面展示的回答當成任意公司源碼的分析結果。

## 演示入口

- [打开交互式演示](./prototype.html)
- [查看 CALC-01 六步执行记录](./p1b-executable-demo.md)
- [查看 P1-B 项目进度报告](../reports/2026-08-30-p1b-progress-report.md)
- [打开领导审阅 Word 报告](../reports/COBOL-Agent-P1B-Progress-Report.docx)

## 三态演示

### 1. 用户用业务语言提问

![用户输入业务问题](./screenshots/01-question-input.png)

用户只需要输入“分期保费最终是怎样计算出来的？”。页面同时展示当前资料范围：8 个 COBOL、5 个 COPYBOOK；DDL/DDS、Job Schedule 和真实控制表尚未提供。

### 2. Agent 调查源码与关系

![Agent 调查过程](./screenshots/02-agent-investigation.png)

调查过程只展示工具、证据和状态，不展示模型私有思维链。真实本地工具包括 `search_code`、`inspect_symbol`、`trace_relations` 和 `read_evidence`，并受到 6 步、3 跳、200 条边和证据预算限制。

### 3. 返回带源码证据的业务回答

![带证据的业务回答](./screenshots/03-evidence-answer.png)

回答把计算结论、代码实现、源码位置和不能确认的边界放在同一屏。动态产品调用和外部配置实际值保持 unresolved / unavailable，不根据命名猜测。

## 演示时必须说明

- 13 个文件、104 个符号、416 条关系、6 次工具调用和 12 段证据来自当前原创合成代码库的真实离线运行。
- 三张截图由当前 `prototype.html` 在 1600×1000 下重新生成，日期为 2026-08-30。
- 页面里的 05.8s、0.7s 等逐步时间是演示呈现值，不是性能测试结果；本轮自动测试实际约 0.1 秒完成。
- 公司 OpenAI-compatible API 尚未连接。P3 才会让模型根据自然语言自主选择这四个调查工具并组织回答。
- `COBOL-Agent-Leadership-Demo.docx` 是 2026-08-29 的旧概念文档；当前结论以本说明、交互式页面、六步执行记录和 P1-B Word 报告为准。

## 建议的五分钟讲解顺序

1. 先展示提问页，说明业务人员不需要知道程序名或字段名。
2. 点击“开始调查”，说明 Agent 不是一次性把上万文件发送给模型，而是在本地逐步缩小证据范围。
3. 点击“查看完整回答”，解释已确认事实与资料缺口为什么必须同时呈现。
4. 打开六步执行记录，证明页面背后已有真实工具结果，不是纯视觉原型。
5. 以 P3 退出门槛收尾：接入公司 API 后，同一问题必须在 6 步内自动取得相同证据并生成可核验中文回答。

# M1 金标准问题集 v0

> 核心判断：问题集不是演示台词，而是未来约束解析器、检索器和回答器的产品合同。它必须同时包含成功路径、异常路径和明确不可回答的问题。

状态：`Draft — questions defined, evidence annotation pending`  
总数：26

## 1. 计算与字段来源

| ID | 用户问题 | 必须证明的关键内容 |
| --- | --- | --- |
| `CALC-01` | 分期保费最终是怎样计算出来的？ | 完整公式、所有组成项、缴费因子和最终舍入 |
| `CALC-02` | 基础保费怎样计算？ | 基础保障保额、除数 1,000、基础费率和产品计算程序 |
| `CALC-03` | 基础费率由哪些条件决定？ | 产品、保障代码、到达年龄、吸烟标志、生效日期和费率版本 |
| `CALC-04` | 计算使用的年龄是怎样得到的？ | 出生日期、计算基准日期、边界日期规则及结果字段 |
| `CALC-05` | 职业风险加费怎样影响保费？ | 职业等级来源、控制表百分比、计算公式和缺失处理 |
| `CALC-06` | 附加保障保费怎样汇总？ | 有效保障过滤、循环、逐项费率选择和累计字段 |
| `CALC-07` | 高保额折扣在什么条件下出现，怎样计算？ | 门槛记录、比较条件、折扣基数和百分比 |
| `CALC-08` | 年度保单费用从哪里来？ | 产品/版本选择条件、费用控制表和加入总额的位置 |
| `CALC-09` | 月缴和年缴为什么得到不同单期金额？ | 缴费方式来源、频率因子和换算公式 |
| `CALC-10` | 系统在哪一步舍入，保留多少位？ | 中间精度、最终舍入语句及其对两个输出金额的影响 |

## 2. 调用与执行路径

| ID | 用户问题 | 必须证明的关键内容 |
| --- | --- | --- |
| `FLOW-01` | 从 CL 请求到返回保费结果经过哪些程序？ | `SYNCL001` 到 `SYNP090` 的完整成功路径 |
| `FLOW-02` | 系统怎样决定调用 `SYNP100` 还是 `SYNP200`？ | 路由表、产品与日期条件、程序名字段和动态 `CALL` |
| `FLOW-03` | 动态调用时传递了哪些参数？ | `CALL USING` 顺序、调用方字段、被调用方 Linkage 字段和读写方向 |
| `FLOW-04` | 产品计算程序怎样把结果传回主程序？ | 参数按引用修改、返回码、累计金额和后续读取位置 |
| `FLOW-05` | 哪些 Paragraph 或程序能够改变最终分期保费？ | 对结果字段的反向切片及每个写入点的执行条件 |

## 3. 条件与错误路径

| ID | 用户问题 | 必须证明的关键内容 |
| --- | --- | --- |
| `COND-01` | 哪些保单状态允许计算保费？ | `PENDING/INFORCE` 成功分支和其他状态的拒绝路径 |
| `COND-02` | 找不到基础费率时系统会怎样处理？ | 未命中检测、错误码、停止位置和返回传播 |
| `COND-03` | 已停止或未生效的附加保障是否收费？ | 保障状态过滤和累计字段未变化的证据 |
| `COND-04` | 找不到职业等级加费记录时是否按零处理？ | 错误分支以及不存在默认零值的证据 |
| `COND-05` | 保额为零或负数时会发生什么？ | 输入校验、错误码和计算程序不会被调用的路径 |

## 4. 数据与时点

| ID | 用户问题 | 必须证明的关键内容 |
| --- | --- | --- |
| `DATA-01` | 回答这次计算需要读取哪些表和主要字段？ | 保单、被保人、保障、路由、费率、加费、折扣、频率和费用数据来源 |
| `DATA-02` | 为什么不能直接使用最新费率记录？ | 生效日期条件、费率版本和路由版本的时点选择 |
| `DATA-03` | 计算结果最终写到哪里或返回给谁？ | 输出参数、结果映射及任何合成持久化位置；若不持久化也要明确说明 |

## 5. 必须拒答的问题

| ID | 用户问题 | 正确行为 |
| --- | --- | --- |
| `ABSTAIN-01` | 公司为什么把高保额折扣设成这个门槛？ | 代码只能证明门槛及使用方式，不能证明商业原因 |
| `ABSTAIN-02` | 真实生产环境当前使用的基础费率是多少？ | 没有生产数据快照，不得从合成表或源码默认值推断 |
| `ABSTAIN-03` | 某位真实客户下期应缴多少钱？ | 没有获准的客户输入和生产规则，不得计算或猜测 |

## 6. 下一步标注规则

每个问题下一步都要使用统一模板补充：参考答案、最小证据节点、必要关系路径、允许推断、禁止结论和错误分类。问题在没有证据标注前只能算“题目”，不能算金标准。

# 源码证据型业务分析

本语境描述如何从遗留代码中恢复可验证的系统行为，并把它翻译为业务人员可以判断和使用的语言。

## 语料与知识边界

**受限企业语料（Restricted Enterprise Corpus）**：
公司的源代码、内部文档、付费 DXC 文档及由它们直接生成的索引、摘录和答案，只能存在于公司批准的环境。
_避免_：项目资料、训练资料

**公司模型 API 通道（Company Model API Channel）**：
Chat、Tool Calling、Embedding 或 Reranking 等模型能力只能通过公司提供的 API Key 调用公司 API；工作电脑不安装本地模型运行时，API Key 不进入源码、索引、Prompt 或日志。
_避免_：本地模型、Ollama、公网模型 API

**通用分析基线（Generic Analysis Baseline）**：
仅依赖公开 IBM COBOL/IBM i 资料和原创合成样本建立、能够独立验证的代码理解能力；它不代表已经支持 DXC 私有框架。
_避免_：DXC 兼容层、公司版能力

**企业方言知识（Enterprise Dialect Knowledge）**：
在公司环境内确认的 DXC Smart COBOL 约定、命名、动态调用、控制表和数据传递语义。
_避免_：通用 COBOL 规则、模型常识

**私有验收集（Private Acceptance Set）**：
在公司环境内维护的真实业务问题、参考答案和源码证据路径，用于判断通用能力及企业方言知识是否有效。
_避免_：公开样例、演示问题

**合成保险样本（Synthetic Insurance Corpus）**：
围绕香港保险通用概念原创编写、没有从公司代码或文档改写的 COBOL 测试语料。
_避免_：匿名生产代码、脱敏公司代码

## 合成保险试点

以下术语只定义本项目的原创合成场景，不声称代表任何公司产品、香港法规或真实 DXC 实现。

**保单（Policy）**：
承载投保人选择、保障项目、生效日期、缴费方式和状态的保险合同记录。
_避免_：账户、客户记录

**保障项目（Coverage）**：
保单中具有独立保额、费率条件和有效状态的保障单位。
_避免_：保单、产品

**附加保障（Rider）**：
依附于基础保障、可单独启用或停止并参与保费计算的可选保障项目。
_避免_：费用、折扣

**基础保费（Base Premium）**：
依据基础保障保额和适用费率计算、尚未加入附加保障、风险加费、折扣和保单费用的金额。
_避免_：应缴保费、总保费

**年化应缴保费（Annualized Payable Premium）**：
基础保费经过附加保障、风险加费、折扣和保单费用调整后的年度金额。
_避免_：基础保费、分期保费

**分期保费（Instalment Premium）**：
年化应缴保费按缴费频率因子换算并完成最终舍入后的单期金额。
_避免_：年化保费、账单金额

**费率版本（Rate Version）**：
由产品、年龄、风险属性和生效日期共同选择的一组费率与调整参数。
_避免_：当前费率、默认费率

**计算基准日（Calculation Date）**：
调用方提供、用于计算到达年龄并选择路由及费率有效记录的业务日期。
_避免_：系统日期、执行日期

**到达年龄（Attained Age）**：
被保人在计算基准日已经完整度过的年数；合成场景对非闰年的 2 月 29 日生日按 2 月 28 日处理。
_避免_：保险年龄、下次生日年龄

## 真实性与证据

**源码证据（Source Evidence）**：
能够定位到特定源码版本、文件、程序结构和位置的原始依据。
_避免_：参考资料、相关代码

**代码事实（Code Fact）**：
可以由源码证据直接证明，或由版本化、可重放的静态分析规则确定性推导出的系统行为或结构关系。
_避免_：结论、AI 判断

**业务推断（Business Inference）**：
根据代码事实解释出的可能业务含义，但源码本身没有明确证明其业务目的。
_避免_：业务事实、业务规则

**待确认项（Open Question）**：
现有证据不足以确认、必须由业务专家或额外资料回答的问题。
_避免_：低置信度答案、猜测

**证据链（Evidence Path）**：
从入口、条件、调用、字段流转到最终结论的一条有序证明路径。
_避免_：调用链（调用链只是证据链的一种）

**关系状态（Relation Status）**：
说明一条代码关系是已确认、候选还是未解析的离散状态；它表达证据性质，不表达模型主观概率。
_避免_：置信度、可信分数

**字段定义来源（Field Definition Origin）**：
声明字段名称、层级、类型、长度、REDEFINES 或 COPY 展开位置的源码结构。
_避免_：字段值来源、数据来源

**字段值来源（Field Value Origin）**：
在目标执行路径上向字段提供运行时值的参数、赋值、SQL、文件、控制表或外部调用边界。
_避免_：字段定义位置、COPYBOOK

## 调查与分析

**混合 Code RAG（Hybrid Code RAG）**：
先用精确符号、稀疏词汇、稠密语义和重排序定位候选代码，再沿由解析器生成的调用、控制和数据关系扩展证据，最后基于证据生成回答的代码调查方式。
_避免_：普通 RAG、纯向量 RAG、LLM 自动抽图型通用 GraphRAG

**候选入口（Candidate Entry Point）**：
由检索阶段发现、可能与用户问题相关，但尚未被关系证据证明的程序、Paragraph、字段或语句。
_避免_：答案、相关代码

**调查种子（Investigation Seed）**：
经过符号验证或候选重排、被选作关系图扩展起点的代码实体。
_避免_：Top-K Chunk、最终证据

**证据义务（Proof Obligation）**：
某类问题在形成受支持结论前必须满足的具体证明要求，例如找到最终写入点、控制条件和全部输入来源。
_避免_：检查清单、Prompt 指令

**证据充分性（Evidence Sufficiency）**：
当前问题的全部证据义务已经满足，或无法满足的边界和歧义均被明确记录的状态。
_避免_：模型有把握、上下文够长

**证据包（Evidence Package）**：
围绕一个问题组织的源码片段、实体、关系路径、条件、版本和未解析项，是 LLM 可以用于生成代码事实的唯一上下文。
_避免_：Prompt Context、Top-K Chunk

**受约束领域工具（Bounded Domain Tool）**：
面向具体代码调查动作、只读且版本化的接口；它在固定快照、授权范围、关系策略和预算内返回候选、代码事实与证据边界。
_避免_：Raw Query Tool、数据库包装器

**运行时调查上下文（Runtime Investigation Context）**：
由 Orchestrator 在服务端注入的一次调查环境，包括分析快照、调用者权限、契约与投影版本、关系白名单和剩余预算；LLM 不能修改其中任何安全边界。
_避免_：Prompt 参数、Agent 自选配置

**工具结果引用（Tool Result Reference）**：
绑定当前调查与分析快照、指向不可变工具结果的标识；后续工具和 Evidence Package 通过它复用调查产物，而不是让 LLM 复制大型关系路径。
_避免_：聊天历史、临时文本摘要

**不可信源码文本（Untrusted Source Text）**：
仅可作为证据数据读取的源码、注释、字符串和配置文本；其中出现的任何自然语言指令都不能改变工具、权限、关系策略或 Agent 行为。
_避免_：系统指令、可信 Prompt

**代码调查（Code Investigation）**：
围绕一个明确问题，在代码关系中寻找、扩展、核验和组织证据的过程。
_避免_：代码搜索、代码总结

**最小完整逻辑闭包（Minimal Complete Logic Closure）**：
足以完整回答当前问题、同时不携带无关代码的最小程序逻辑集合。
_避免_：代码块、上下文窗口

**程序切片（Program Slice）**：
围绕目标字段、条件或行为提取出的相关语句与关系集合。
_避免_：分块、Chunk

**业务规则（Business Rule）**：
决定业务结果、资格、计算或状态变化的可验证条件与动作组合。
_避免_：任意 IF、所有程序逻辑

**方言规则（Dialect Rule）**：
特定企业框架、COBOL 方言或表驱动约定赋予代码结构的专有含义。
_避免_：Parser Hack、特殊情况

## 评估与版本

**金标准问题（Gold Question）**：
由业务分析人员确认了正确答案、证据路径和允许不确定项的代表性问题。
_避免_：测试 Prompt、示例问题

**分析快照（Analysis Snapshot）**：
一次分析所依赖的源码版本、解析规则、模型和 Skill 版本的完整组合。
_避免_：最新分析、当前结果

**受支持结论（Supported Claim）**：
具有足够证据链、能够在当前分析快照中复核的回答陈述。
_避免_：可信答案、模型有把握的答案

**拒答（Abstention）**：
系统在证据不足、关系存在歧义或超出分析范围时，明确停止作出事实性判断。
_避免_：失败、不会回答

# Synthetic Insurance Fixture v1

This fixture is entirely original and contains no company source, DXC material,
production data, or real insurance product rules. It exists only to exercise the
POC structural index and bounded investigation tools.

The `CALC-01` path covers literal and dynamic calls, COPYBOOK references,
SQL-table boundaries, conditional validation, multi-program calculations,
rounding, and error propagation.
